# OfficeNest 思维记录（THINKING LOG）

> **这份文档是干什么的**：记录一次重要的设计讨论（2026-08，v2.3 前后）。
> **给谁看**：① 未来的我自己（记忆力不好，需要这份文档找回上下文）② 接手项目的 AI（先读这个，再读 AGENTS.md）。
> **怎么读**：先看「结论速览」，再看「讨论历程」。
> **格式约定**：`>` 引用块 = 我当时说的话（原话）。`PS（AI 注）` = 当时 AI 的分析和想法。分开记录是为了分清"我的直觉"和"AI 的论证"。

---

## 一、结论速览（不读全文就看这个）

本次讨论围绕一个核心洞察展开：**LLM 擅长"写"和"理解"，不擅长"算"和"记"。把确定性的事交给计算机，把灵活性的事留给 LLM。**

产出了 3 个能力升级点，1 个被否决的想法，1 个推迟的想法：

| # | 想法 | 状态 | 一句话 |
|---|---|---|---|
| 1 | **公式计算引擎** | ✅ 已实现 | LLM 写表达式，计算机安全求值（AST 白名单） |
| 2 | **方法论体系** | 📐 设计中 | 三层能力金字塔：工具 → 工作流 → 方法论 |
| 3 | **知识点焦点阅读** | 📐 设计中 | 方法论的学习模式：不通读全文，以知识点为核心聚焦阅读 |
| 4 | 动态工具注册 | ❌ 否决 | LLM 运行时写代码注册工具——安全模型不成立 |
| 5 | SQL 后台化 | ⏸ 推迟 | Excel 数据转 SQL 缓存——方向对但当前场景收益低 |
| 6 | 测试策略 | 📐 已讨论 | 确定性层单元测试，LLM 层契约校验，最终人工验收 |

**依赖关系**：公式引擎（1）是方法论（2）的计算底座；知识点阅读（3）是方法论的学习模式。3 建立在 2 之上，2 建立在 1 之上。

**PS（AI 注）——为什么这几个点值得做**：它们共享同一个设计哲学——"LLM 负责表达意图，程序负责执行确定性的部分"。公式引擎把"算术"从 LLM 手里拿走，方法论把"分析框架的组织"交给存储和引擎，焦点阅读把"通读全文"变成"按需检索"。三者都不引入新依赖，全是标准库 + 已有基础设施（SQLite/ChromaDB/事件总线）的重新组合。这个项目的 60 个工具已经证明了"工具化"的价值，这三个点是在验证"**结构化能力**"的价值——工具解决单点，方法论解决流程，焦点阅读解决学习。

---

## 二、讨论起点：我的痛点

我做 OfficeNest 是因为要处理 76 份员工合同（我是 HR 实习生，非程序员）。项目 100% AI 辅助编写。

我注意到一个规律：**大模型的算术能力，尤其是面对复杂公式其实是不佳的**。它会"自信地算错"——百分比、多步运算、货币转换，全都会错。所以我想：为什么不让它写公式，让计算机算？

> 我："大模型的算术能力，尤其是面对复杂公式其实是不佳的，为何不让其写入公式，然后填入对应数字后交由计算机进行计算"

**PS（AI 注）——为什么这是个好问题，以及它比想象中更值钱**：
- 表面看是在解决"算不准"，但本质是在问："哪些事情不该让 LLM 做？"这个问题的答案目录会越来越长——算术只是第一个。
- 关键洞察不是"算得快"，而是**上下文经济**：LLM 读 1000 行 Excel 数据进上下文要 2 万 token，写一个表达式让引擎算只要 50 token。这不是优化速度，是优化"数据进入上下文的方式"。
- 我的判断依据：当时项目里 `excel_find_row` 反复出现"数错行"的历史 bug（A1 只读），说明 LLM 在"精确处理大量数据"上结构性不可靠。公式引擎是第一个系统性的解药。

---

## 三、升级点 1：公式计算引擎（已实现 ✅）

### 讨论过程（我的两次修正）

1. **AI 最初设计**：内置财务/统计函数库（PV/FV/PMT 那种）。
2. **我的修正**：不需要内置公式——公式是 LLM 的知识（个税公式、圆面积它都会写），我们只负责确保运算过程不出错误。

> 我："这些公式不需要自带，而是可以让 llm 自己写，我们更多的是确保运算过程不出错误"

**PS（AI 注）——这个修正比看上去重要**：内置公式库的隐含假设是"AI 知道所有可能的公式"，这是错的——AI 的公式知识是无限的，硬编码反而会限制它。用户直觉点破了"LLM 是知识源，程序是计算器"的正确分工。这个分工后来成为整个讨论的主线。

3. **我的第二次修正**：不只是会计财务，还要覆盖数学、计算机等更复杂的公式——要尽可能通用化。

> 我："这不仅仅是会计和财务，也包含数学、计算机等更加复杂的公式，so，我们需要把这个东西做的尽可能通用化"

**PS（AI 注）——通用化直接改变了技术选型**：
- 如果只做会计，sympy（符号数学库）就够了；但位运算、逻辑运算、进制转换这些"计算机味"的需求，sympy 完全不支持。
- 于是从"符号数学"转向"受限 Python 求值"——Python 表达式语法本身是 LLM 最熟悉的语言，天然支持一切运算。
- 通用化的代价是安全问题变得更尖锐：能跑任意表达式，就得防任意代码。这引出了 AST 白名单方案。

### 最终设计

- **表达式语法**：Python 语法（LLM 最熟，天然支持幂 `**`、位运算 `&|^`、逻辑、math 函数）
- **安全层**：AST 节点白名单——拒绝 `Attribute`（`a.b`）、`Subscript`（`a[b]`）、`__import__` 等逃逸后门。纯数学表达式，杜绝任意代码执行
- **函数白名单**：math 标准库 + 几个内建（abs/round/sum/bin/hex...），能力可扩展
- **存储**：SQLite 公式注册表——写一次，按名复用

**PS（AI 注）——为什么 AST 白名单是安全的本质**：
- 裸 `eval()` 能执行任意代码，`().__class__.__bases__` 这种"属性链攻击"可以一路摸到 `os`。AST 白名单在**执行前**就把 `Attribute` 和 `Subscript` 节点全部拒绝——没有属性访问，就没有逃逸路径。
- 函数白名单是第二道闸：即使表达式合法（比如 `sin(x)`），`sin` 也必须在我注册过的函数表里，未注册的函数直接报错。
- 两道闸都是**确定性**的——不是概率防御，是结构上不允许。这是"LLM 写代码"场景下唯一能接受的防御等级。

### 已实现内容

- `adapters/formula_store.py` — 公式增删查（SQLite）
- `skills/formula_skill.py` — 4 工具：`formula_save` / `formula_list` / `formula_calc` / `formula_eval`
- `main.py` 注册 + System Prompt 规则 0：**任何数学计算禁止心算，写表达式交给计算机**
- 安全测试通过：`__import__("os")`、`().__class__.__bases__` 均被拒绝
- commit `ef204f7`（push 当时网络失败，需补推）

### 使用示例

```
用户: 张三工资15000，个税多少？税率10%，速算扣除210
AI:   formula_eval("收入*税率-速算扣除", {"收入":15000, "税率":0.1, "速算扣除":210})
     → ✅ 结果: 1290.0
AI:   formula_save("个税", "收入*税率-速算扣除", "工资个税计算")
     → 下次直接 formula_calc("个税", {...})
```

### 技术选型备忘（为什么不用别的）

| 候选 | 为什么不用 | 实际用的 |
|---|---|---|
| 裸 `eval()` | 极危险，任意代码执行 | — |
| `ast.literal_eval` | 只能算字面量，不能算表达式 | — |
| `sympy` | 符号数学强，但位运算/逻辑/统计不支持，第三方依赖 | — |
| `asteval` | 合适但需额外安装 | — |
| **AST 白名单 + 白名单函数表** | — | ✅ **纯标准库**（ast/math/sqlite3），零新依赖 |

**PS（AI 注）——零依赖是决定性优势**：这个项目要打包成 exe 发给同事，每多一个依赖就多一分打包崩溃风险。纯标准库意味着在任何 Python 3.11+ 环境都能跑，不会遇到"缺库"问题。

---

## 四、升级点 2：方法论体系（设计中 📐）

### 我的原始想法

在工具之上加两种工作方式——**工作流**（微观固定逻辑）和**方法论**（宏观分析方法，由多个公式和知识点组成，例如经济分析和会计分析）。

> 我："我认为除此之外还应当有两种工作方式，即工作流和方法论，分别是微观层面的固定逻辑方案和宏观层面的分析方法，后者可以由多个公式和知识点组成，例如经济分析和会计分析"

**PS（AI 注）——为什么这个想法是"架构级的"**：
- 用户没有说"加一个工具"，而是说"加一种能力层级"——这是从"工具集合"向"能力体系"的跃迁，正是 Harness 类产品（如 DeepSeek Harness）的设计思路。
- 直觉准确的地方：工作流（步骤序列）和方法论（分析框架）确实是两个不同抽象层级——前者回答"怎么做"，后者回答"怎么想"。AI 独立补充的"解读规则"（interpretation）是方法论区别于工作流的关键：分析不仅要算，还要"算完怎么看"。

### 三层能力金字塔

```
┌─────────────────────────────────────┐
│  🧭 方法论（宏观分析方法）           │  ← 新的一层
│  经济分析 / 会计分析 / 报表解读      │
│  = 多个公式 + 知识点 + 步骤 + 解读   │
├─────────────────────────────────────┤
│  🔄 工作流（微观固定逻辑）           │  ← 已有 workflow_skill
│  保存操作序列，下次复用              │
├─────────────────────────────────────┤
│  🔧 工具 + 🧮 公式（原子能力）       │  ← 已有 60 工具 + 公式引擎
└─────────────────────────────────────┘
```

### 方法论 ≠ 工作流

| | 工作流 | 方法论 |
|---|---|---|
| 层级 | 微观 | 宏观 |
| 内容 | 工具调用序列 | 公式 + 知识点 + 分析步骤 + 解读规则 |
| 回答 | "怎么做" | "为什么这么分析 + 用什么算 + 算完怎么解读" |
| 例子 | excel_read → excel_write | 杜邦分析（ROE 分解为净利率×周转率×权益乘数，再按阈值解读） |

### 方法论的数据结构

```json
{
  "name": "杜邦分析",
  "description": "ROE 分解：盈利能力×运营效率×财务杠杆",
  "inputs": ["净利润", "营业收入", "总资产", "净资产"],
  "formulas": [
    {"name": "净利率", "expr": "净利润/营业收入", "note": "衡量盈利能力"},
    {"name": "ROE", "expr": "净利率*总资产周转率*权益乘数", "note": "净资产收益率"}
  ],
  "knowledge": "杜邦分析将 ROE 拆解为三个驱动因素...",
  "interpretation": [
    {"condition": "ROE > 15%", "meaning": "股东回报优秀"},
    {"condition": "权益乘数 > 3", "meaning": "杠杆过高，偿债风险大"}
  ]
}
```

**PS（AI 注）——为什么公式要存"表达式"而不是"结果"**：存表达式意味着方法论是**可重算的**——同一套分析框架，换一批数据（下个季度、另一家公司）直接复用。如果存结果，方法论就死了。

### 执行流程

```
methodology_run("杜邦分析", {净利润: 5000万, ...})
  → ① 公式逐条算（FormulaEngine，确定性）
  → ② 结果 + knowledge 丢给 LLM → 结合知识点写解读（LLM 擅长）
  → ③ 输出分析报告：数字表格 + 人话解读
```

**确定性计算 + 灵活性解读，各干各擅长的。**

**PS（AI 注）——执行时的分工是刻意设计的**：公式必须程序算（LLM 会算错），解读必须 LLM 写（规则引擎写不出有洞察的分析）。这个分工在讨论中确认过，未来实现时不要改成"全规则"或"全 LLM"。

### 拟实现工具

`methodology_learn` / `methodology_relearn` / `methodology_list` / `methodology_run` / `methodology_export`

### 关键：方法论需要"学习过程"

我提出：方法论不能手工定义，**直接丢超长文章给 AI，让它总结并结合上下文形成分析方法**。

> 我："我感觉方法论和工作流，这俩都需要学习过程，尤其是方法论，可以直接丢超长文章给AI让其总结并结合上下文形成分析方法"

**PS（AI 注）——这句话定义了方法论的"生产方式"**：不是 AI 开发者写死，而是 LLM 从资料中提炼。这有个隐含前提——LLM 的提炼必须**结构化**（JSON），否则没法存、没法校验、没法复用。所以 learn 的产物必须是严格 schema 的 JSON，这是后续所有设计的锚点。

AI 的补充：这与现有知识库有本质区别——知识库是"存原文、查原文"，方法论学习是"围绕知识点聚拢+提炼"，是从知识到能力的转化。

> **PS（AI 注）——知识库 vs 方法论的核心区别**：
> - 知识库：存原文 chunk，检索时返回原文片段——"知道有这回事"
> - 方法论：提炼成可执行框架（公式集/步骤/解读）——"知道怎么用它干活"
> - 同一个文档，知识库回答"资料里说过什么"，方法论回答"怎么用资料里的方法做事"

---

## 五、升级点 3：以知识点为核心的阅读（设计中 📐）

### 讨论演进（我的三次修正）

1. **初版**：一次性总结全文 → AI 指出太笼统，注意力稀释
2. **修正 1**：按章节提炼 → 每章独立提炼，结构清晰，可局部重学

> 我："我感觉需要按章节提炼的功能"

**PS（AI 注）——章节提炼的三个额外收益**：① 每章聚焦，提炼质量高 ② 章节=结构，方法论天然分层 ③ 可追溯——"这个公式来自哪章"，发现某章提炼错了只重学那章。但章节是文档结构，不是知识结构——同一知识点可能散在多章，这为下一轮修正埋了伏笔。

3. **修正 2（最终形态）**：以知识点为核心阅读——每次从一个知识点出发，聚焦与其关联的内容，而非理解所有部分。

> 我："能否让AI以知识点为核心进行阅读，每次从一个知识点出发聚焦与其关联的内容和知识，而非理解所有部分？"

**PS（AI 注）——这是整个讨论中最有洞察的一句话**：
- 它把"阅读"从**线性通读**变成**焦点式检索**——人读书就是这样：带着问题翻书，只看相关章节。
- 技术上的对应物是 RAG（检索增强），但用户是**从认知层面**独立想到的，没有用任何术语——"外行但深刻"的典型例子。
- 它把方法论学习的成本结构改变了：通读全文（token 大）+ 每个知识点聚焦阅读（token 小），总量省 60-80%，且理解更深。
- 代价：焦点阅读可能漏掉"隐性联系"（第 3 章的例子暗合第 7 章的结论，只有通读才可能发现）。所以目标明确时用焦点阅读，无目标探索时需通读兜底。

### 焦点阅读流程

```
输入：超长文章 + 目标领域
  │
  ▼
① 轻量扫描全文 → 识别知识点清单（只做目录理解，token 开销小）
  [知识点1: 净利率计算, 知识点2: 杜邦分析, 知识点3: 杠杆风险...]
  │
  ▼
② 逐知识点聚焦阅读（每个知识点一次调用）：
   → 从全文定位相关片段 → 只读这些片段 → 提炼
   {formulas, knowledge, steps, 关联: [知识点A, 知识点B]}
  │
  ▼
③ 构建知识点关联图谱（谁引用谁）
  │
  ▼
④ 汇总成方法论 → FormulaEngine 校验公式 → 存 SQLite
```

### 关键认知

- **章节是组织模式，知识点是学习模式**——两者并存：知识点提炼时标注来源章节，方法论按章节组织展示，按知识点关联执行
- **收益**：token 省 60-80%，理解更深，方法论从线性步骤升级为网状知识
- **代价**：目标明确时完美；无目标探索时会漏掉隐性联系（需要通读兜底）

**PS（AI 注）——关联图谱的价值**：执行方法论时，LLM 可以顺着关联走——"杜邦分析需要净利率，净利率怎么算？→ 查知识点 1"。这使方法论从"一份文档"变成"一张可导航的知识网"。

---

## 六、被否决的想法：动态工具注册（❌）

我提出：**给这玩意加上"他自己可以写功能的功能"**——LLM 遇到现有工具覆盖不了的任务，自己写 Python 代码注册成新工具。

> 我："或许我们应该给这玩意加上他自己可以写功能的功能？"

### 为什么否决

我很快意识到：**这个功能极为不可靠，本身也是困难重重**。

> 我："但是这个功能极为不可靠，本身也是困难重重"

**PS（AI 注）——否决的真正理由（讨论中的关键论证）**：

**安全模型的核心假设是"人能审查代码"——而我们的项目恰恰没有这个条件。** 注册前弹窗让用户看代码？使用者是非程序员（我和我的同事），让乘客检查飞机引擎不现实。

| 问题 | 现实 |
|---|---|
| 注册时测过 ≠ 运行时正确 | 边界情况、中文路径、编码——用第 50 次可能就炸 |
| 错误不可读 | 动态工具报错，分不清是 AI 写的还是系统的 |
| 自测自夸 | LLM 给自己写测试用例，通过率没有参考价值 |
| 工具泛滥 | 静态工具是精选的，动态工具会积累烂代码 |

**DeepSeek 敢做动态注册，是因为它有内部 benchmark、有工程师审代码、有测试集。我们一个都没有。**

**PS（AI 注）——为什么"没有测试集"是硬伤**：动态工具注册的完整闭环需要"注册→验证→进化"，验证依赖测试基准。我们没有基准，意味着任何注册的工具都无法证明"比不注册好"。这个功能的价值在验证环节断裂，而不是写代码环节。

### 降级方案（未实现，供参考）

一次性 `python_run` 脚本执行工具——用完即焚，不持久化不注册，走 powershell_run 同款确认弹窗。LLM 负责写，程序负责跑，跑完就完事。

**PS（AI 注）——降级方案的意义**：它保留了"LLM 写代码"的灵活性，但把"留存的工具"（有安全面）变成"一次性的脚本"（无安全面）。哲学一致：LLM 负责写，程序负责跑，跑完不留下任何东西。

---

## 七、推迟的想法：SQL 后台化（⏸）

我提出：LLM 反复观察 Excel 表格，为何不在它认为内容可转 SQL 时转换格式写入临时 SQL，操作时同步 Excel？

> 我："sql本质是将这个过程后台化，进行数据提取"

**讨论结论**：
- SQL 买的不是"新能力"，是**确定性计算**（COUNT/AVG 不会算错）+ **上下文经济**（过滤在引擎里发生，只回结果）
- 但适用面窄：精确查找/统计适合，通读找规律反被碍事，小表白费劲
- 当前场景（写多读少，录入 76 份合同）收益低，**优先级不高**

**PS（AI 注）——这个讨论澄清了一个重要概念**：SQL 不改变 LLM 的"能力"，改变的是"计算发生的位置"。过滤/聚合从"LLM 的上下文里"（概率性、贵）挪到"SQLite 引擎里"（确定性、便宜）。这个"计算位置"的心智模型，后来直接用在公式引擎（计算从 LLM 挪到 Python）和方法论（公式从 LLM 挪到引擎）上——**SQL 讨论是公式引擎的思想源头**。

> 我："可惜我目前的工作完全用不到这个……我们的工具已经很稳定了，项目正处在我到处发给别人测试的阶段"

---

## 八、测试策略（📐 已讨论）

> 我："问题是，没法测试"

**PS（AI 注）——这句话切中了 LLM 应用测试的本质困境**：确定性系统可以测试（同样的输入同样的输出），LLM 输出是概率性的，没法断言。但这个困境的解法不是"不测"，而是**分层**：

**结论：能测的测死，不能测的用契约兜住，最后的判断交给人。**

| 层 | 策略 |
|---|---|
| 引擎/存储/执行/公式校验 | 单元测试（确定性断言）—— `sqrt(144)+sin(pi/2)==13.0` 这种永远跑得过 |
| LLM 提炼结果 | **契约校验**：JSON schema（必须有 inputs/formulas/knowledge）、每个 expr 必须能被 FormulaEngine 求值、公式变量必须能在 inputs 找到、interpretation 条件必须合法 |
| 最终质量 | 人工验收（learn 完看摘要确认）+ 快照回归（golden file 对比，看有没有意外变化） |

**PS（AI 注）——契约校验为什么能拦住 90% 的错误**：LLM 提炼结果最常见的错误不是"内容不对"，而是"结构不对"——JSON 格式坏、公式表达式写错、变量没定义、interpretation 条件非法。这些全部是**确定性的、可编程校验的**。真正拦不住的是"内容深度不够"这种主观问题，那部分交给人工验收。**LLM 负责写，校验是代码做——这个分工和公式引擎的哲学完全一致。**

**待办**：写 `tests/test_formula.py`——项目第一个测试文件（尚未写）。

---

## 九、对整个项目的可行性评估

### 我为什么觉得这些方向可行（我的判断）

1. **公式引擎已实现并验证**——安全测试全过，纯标准库零依赖。这条路证明"LLM 写、程序算"的模式成立
2. **方法论复用已有积木**——公式引擎（算）、SQLite（存）、workflow 模式（组织）、ChromaDB（检索），没有任何新引擎
3. **我是在测试阶段发现的痛点**——60 工具稳定了才有余力想这些，说明项目成熟度到了

### 优先级评估（AI 的建议）

| 优先级 | 事项 | 理由 |
|---|---|---|
| ⭐⭐⭐ | 公式引擎单元测试 | 确定性，立刻能落地，项目第一个测试 |
| ⭐⭐ | 方法论体系 | 依赖公式引擎，设计已完整 |
| ⭐⭐ | 知识点焦点阅读 | 方法论的学习模式，最后一层 |
| ⭐ | 动态工具注册 | 已否决，不做 |
| ⭐ | SQL 后台化 | 推迟，场景不匹配 |

### 风险评估

1. **LLM 提炼质量不可控** → 契约校验 + 人工验收兜底
2. **方法论存储膨胀** → 按需 learn，可 delete
3. **scope creep** → 方法论是"锦上添花"，核心 60 工具不动

**PS（AI 注）——最大的风险其实不是技术**：是"方法论体系"做出来后没人用。工具类功能（公式计算）是用户主动触发的，方法论是"知识资产"，需要积累才显价值。建议：先做一个真实的方法论（比如你自己领域的"合同条款审查流程"或"工资计算流程"），跑通一次完整 learn→run 闭环，比设计 10 个空架子有价值得多。

---

## 十、给未来 AI 的注意事项

1. **先读 AGENTS.md 再读本文件**——本文件记录"为什么"，AGENTS.md 记录"是什么"
2. **公式引擎已实现**：`skills/formula_skill.py` + `adapters/formula_store.py`，commit `ef204f7` 可能未推（当时网络失败，检查 `git push` 状态）
3. **方法论和知识点阅读是设计稿，不是代码**——若后续实现，遵循本文档的数据结构和执行流程
4. **动态工具注册已否决，不要再提议**——理由见第六章，除非项目引入代码审查能力
5. **我的身份**：非计算机专业 HR 实习生。解释技术时用大白话，我读不懂纯术语
6. **我的特点**：记忆力差，需要文档化；直觉不错，会提出"外行但深刻"的想法；讨论时先说清楚再动手
7. **我目前的状态**：项目在测试阶段，到处发给别人测试；使用 DeepSeek V4（glm-5.2 当前模型），关注 DeepSeek Harness 动态
8. **交流风格**：直接、简洁、不要催促（见移交文档开发者备注）
9. **我的思维模式**：我会先给结论（"要做 X"），再在讨论中修正（"不，应该是 Y"）。AI 的职责是**论证和纠偏**，不是顺从——我明确提出过"这个功能不可靠"这样的自我否定，AI 应该尊重并深化这种自我怀疑
10. **有用的讨论模式**：先讨论清楚再动手；技术选型要写明"为什么不用别的"；评估要分"我的直觉"和"AI 的论证"两层

---

*文档创建：2026-08-05。内容为一次设计讨论的完整记录，供未来恢复上下文使用。*
